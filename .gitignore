# İşletim sistemi
.DS_Store
Thumbs.db

# IDE
.idea/
*.iml
.vscode/

# Java derleme çıktıları — lab kaynak kodu depoya girer, derlenmişi girmez
target/
build/
*.class
*.jar

# Yerel ortam
.env
.env.local
*.log

# Kurs klasörünün eski git geçmişi (bkz. README) — devre dışı bırakıldı, izlenmez
.git-devredisi/

# Reverse engineering lab ortamı — kursun kendisi ReverseEngineering/ klasöründe.
# Burada indirilen Ghidra (352 MB), derlenmiş ikili dosyalar ve lab çıktıları
# duruyor; hiçbiri kurs içeriği değil ve depoya girerse senkronun indirdiği
# tarball'ı gereksiz yere şişirir.
ReverseEnginering/
